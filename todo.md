1. fix enable/disable keymappings 

2. make sure the cursor not come out of the window if disableCursor()

3. fix naming convention of raylib_init.c , raylib_init.h 

4. make the code more modular and focus on memory management.

5. change raylib_win_resize_screen function location to key_mapping.c .
